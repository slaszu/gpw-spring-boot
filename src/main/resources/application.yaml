spring:
  datasource:
    url: jdbc:h2:file:~/data/h2/gpw
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update

    show-sql: true
    properties.hibernate.format_sql: true
  h2:
    console.enabled: true
  config:
    import: optional:classpath:gpw.yaml

logging:
  level:
    pl.slaszu.gpw: DEBUG